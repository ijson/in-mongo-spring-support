package ${packager}.query;

import lombok.Data;
import java.util.List;

@Data
public class ${name}Query {

     <%
        for(col in columns){
           print("private "+col.simpleType+" "+col.name+";\n\n");
        }
     %>
}
